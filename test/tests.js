/* eslint-disable no-undef,no-unused-vars */

var assert = require("assert"); // node.js core module

describe('test skeleton', function(){
  describe('first test of many', function(){
    it('fails', function(){
        var retVal = false
      assert.equal(false, retVal); 
    })
  })
});
